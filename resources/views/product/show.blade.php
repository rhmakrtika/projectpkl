{{-- @extends('layouts.app')

@section('content')
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">
                    <div class="float-start">
                        {{ __('Show Product') }}
                    </div>
                    <div class="float-end">
                        <a href="{{ route('product.index') }}" class="btn btn-sm btn-outline-primary">Return</a>
                    </div>
                </div>

                <div class="card-body">
                    <img src="{{ asset('images/products/' . $product->image) }}" class="w-100 rounded">
                    <hr>
                    <h4>{{ $product->name_product }}</h4>

                </div>
            </div>
        </div>
    </div>
</div>
@endsection
​ --}}
